[project]
name = "pose-table-tui"
version = "0.1.0"
description = "Terminal display for live PoseStamped topics over ROS 2"
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "pandas",
    "scipy",
    "tabulate",
    "pyyaml",
]
