[project]
name = "mocap-visualize"
version = "0.1.0"
description = "Terminal display for live mocap poses over ROS 2"
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "pandas",
    "scipy",
    "tabulate",
]
